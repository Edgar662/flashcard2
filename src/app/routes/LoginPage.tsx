/**
 * Placeholder sign-in route. The actual sign-in form and logic belong to
 * the `auth` feature (not yet built) — this page exists so `RequireAuth`
 * has somewhere to redirect to (see docs/10-authentication.md).
 */
export function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Sign in — coming soon.</p>
    </div>
  )
}
