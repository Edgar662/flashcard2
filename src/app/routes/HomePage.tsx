/**
 * Placeholder for "your decks" (docs/08-user-flows.md §1). Proves routing
 * and the auth guard work end to end — the decks feature itself is built
 * in a later module.
 */
export function HomePage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Your decks — coming soon.</p>
    </div>
  )
}
